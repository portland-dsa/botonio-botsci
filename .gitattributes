# Extensionless shell scripts must keep LF endings - a CRLF shebang (`#!/usr/bin/env bash\r`)
# is parsed as a bad interpreter on Linux, so a Windows checkout must not rewrite these.
deploy/bin/deploy-wrapper text eol=lf
scripts/hooks/pre-push    text eol=lf
