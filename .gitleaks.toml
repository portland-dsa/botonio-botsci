# GDI gitleaks, the fake mock discord IDs and the deploy token-NAME enum are not in fact leaks
#
# (ty for looking out for me tho <3)
title = "gitleaks"

[extend]
useDefault = true

[allowlist]
description = "Non-secret definitional strings: fake/public Discord IDs and deploy token-name enums (key names, never values)"
paths = ['''crates/domain/src/ids\.rs''', '''scripts/botonio_deploy/cli.py''']
regexes = []
